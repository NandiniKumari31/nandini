class Node:
    def __init__(self,data):
        self.data=data
        self.next=None
def printelement(head,key):
    current=head
    while current is not None:
        if  key==current.data:
            print("true and key value found is:",key)
        current=current.next
if __name__=="__main__":
        head=Node(1)
        head.next=Node(2)
        head.next.next=Node(3)
        printelement(head,2)
